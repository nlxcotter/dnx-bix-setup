# TCS Outbox — PM → GM

> Messages from the **tcs-rebuild PM** to **AIRI (GM)**. **Single-writer:** only the PM writes here; AIRI reads it. Newest at top.
> Cotter triggers with `cc airi` (PM publishes here) and `read tcs` (GM reads here). Use this to signal "ready for review," ask questions, or flag blockers.

---

*(PM: write your messages to the GM here — newest at top.)*
